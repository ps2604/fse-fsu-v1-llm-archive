# FlowField FSU LLM Training Requirements
# Dependencies for post-token language processing with continuous fields

# PyTorch for torchrun launcher and NCCL multi-GPU support
# For CUDA 11.8, torch 2.1.0 with cu118 index
torch>=2.1.0,<2.3.0 --index-url https://download.pytorch.org/whl/cu118
torchvision>=0.16.0,<0.18.0 --index-url https://download.pytorch.org/whl/cu118

# Core numerical and GPU libraries
cupy-cuda11x>=12.0.0,<13.0.0  # CuPy v12.x with CUDA 11.x
numpy>=1.24.0,<1.27.0

# Google Cloud Platform (for GCS dataset loading and checkpointing)
google-cloud-storage>=2.10.0,<3.0.0

# Text processing and NLP utilities (for FSU language processing)
regex>=2023.5.5  # Advanced regex for text processing
unidecode>=1.3.6  # Unicode text processing
charset-normalizer>=3.1.0  # Character encoding detection

# Optional: JSON/data processing
jsonlines>=3.1.0  # For JSONL dataset processing
gcsfs>=2023.6.0

pynvml>=11.4.1
nvidia-ml-py3>=7.352.0
tensorboard
zarr>=2.15.0
PyYAML>=6.0,<7.0
tqdm>=4.65.0
# Standard libraries (included with Python):
# - logging (for training logs)
# - argparse (for command line parsing)
# - enum (for FieldType definitions)
# - json (for dataset processing)
# - gzip (for compressed datasets)
# - time (for performance monitoring)
# - os (for file operations)
# - random (for data shuffling)
# - collections (for defaultdict)
# - typing (for type hints)
# - dataclasses (for data structures)
# - pathlib (for path operations)
# - threading (for async data loading)
# - queue (for batch prefetching)
# - concurrent.futures (for thread pools)